export class Obstacle {
    constructor(x, y, width, height){
        console.log(y)
        this.x = x
        this.y = y
        this.width = width
        this.height = height
        console.log(this.y)
    }
}